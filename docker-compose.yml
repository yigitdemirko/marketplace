services:
  config-server:
    image: eclipse-temurin:21-jre-alpine
    working_dir: /app
    volumes:
      - ./infrastructure/config-server/target:/app
    command: java -jar config-server-1.0.0-SNAPSHOT.jar
    ports:
      - "8888:8888"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8888/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  discovery-server:
    image: eclipse-temurin:21-jre-alpine
    working_dir: /app
    volumes:
      - ./infrastructure/discovery-server/target:/app
    command: java -jar discovery-server-1.0.0-SNAPSHOT.jar
    ports:
      - "8761:8761"
    depends_on:
      config-server:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8761/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  api-gateway:
    image: eclipse-temurin:21-jre-alpine
    working_dir: /app
    volumes:
      - ./infrastructure/api-gateway/target:/app
    command: java -jar api-gateway-1.0.0-SNAPSHOT.jar
    ports:
      - "8080:8080"
    depends_on:
      discovery-server:
        condition: service_healthy